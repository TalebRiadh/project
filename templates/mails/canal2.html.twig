{% extends 'base.html.twig' %}

{% block body %}
    <h1>Hello {{ name }}!</h1>
    <p>Thank you for subscribing from canal 2.</p>
    <p>Here are your welcome files:</p>
    <ul>
        {% for file in files %}
            <li><a href="{{ file.url }}">{{ file.name }}</a></li>
        {% endfor %}
    </ul>
{% endblock %}

